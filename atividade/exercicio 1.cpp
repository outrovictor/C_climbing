#include <stdio.h>

int main ()

{
    int a = 10 ;
    int b = 2;
    int soma = a+b ;
    int sub = a-b  ;
    int mult = a*b ;
    int div = a/b ;
    int rest = a%b ;

    printf( " As variveis escolhidas foram  = %d e %d \n", a , b  );

    printf( " A soma entre as variaveis e  = %d\n", soma );

    printf( " A subtracao entre as variaveis e  = %d\n", sub );

    printf( " A multiplicacao entre as variaveis e  = %d\n", mult );

    printf( " A divisao entre as variaveis e  = %d\n", div );

    printf( " O resto da divisao entre as variaveis e  = %d\n", rest );

}
